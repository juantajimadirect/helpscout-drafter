{
  "mcpServers": {
    "helpscout": {
      "command": "bash",
      "args": [
        "-c",
        "cmp -s package-lock.json node_modules/.installed-lock 2>/dev/null || { npm ci --no-audit --no-fund >&2 && cp package-lock.json node_modules/.installed-lock; }; npm run build >&2; exec node dist/index.js"
      ],
      "env": {
        "HELPSCOUT_APP_ID": "${HELPSCOUT_APP_ID}",
        "HELPSCOUT_APP_SECRET": "${HELPSCOUT_APP_SECRET}",
        "HELPSCOUT_DEFAULT_INBOX_ID": "${HELPSCOUT_DEFAULT_INBOX_ID:-348804}",
        "AIRTABLE_PAT": "${AIRTABLE_PAT}"
      }
    }
  }
}
