{
  "mcpServers": {
    "helpscout": {
      "command": "bash",
      "args": [
        "-c",
        "cd \"$(dirname \"$0\")\" 2>/dev/null || true; [ -d node_modules ] || npm ci --no-audit --no-fund >&2; [ -f dist/index.js ] || npm run build >&2; exec node dist/index.js",
        ".mcp.json"
      ],
      "env": {
        "HELPSCOUT_APP_ID": "${HELPSCOUT_APP_ID}",
        "HELPSCOUT_APP_SECRET": "${HELPSCOUT_APP_SECRET}",
        "HELPSCOUT_DEFAULT_INBOX_ID": "${HELPSCOUT_DEFAULT_INBOX_ID:-348804}",
        "AIRTABLE_PAT": "${AIRTABLE_PAT}"
      }
    }
  }
}
